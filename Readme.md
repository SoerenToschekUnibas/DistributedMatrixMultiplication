Dense Matrix Multiplication implementation, using single-threaded, OpenMP and Cuda.

The experiments can be repated by running the `multithreaded.sh` script, as well
as the `comparison.job` on the cluster. Experiments were done on the L40s nodes,
however due to the long queues, some single GPU experiments were performed on a local 3090. 

